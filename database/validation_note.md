# Validation Note

- `generate_data.py` was syntax-checked.
- End-to-end generation was smoke-tested on a **tiny validation run** by temporarily reducing counts inside the `small` profile.
- The resulting sample CSVs are included in `exports_tiny_validation/` for quick inspection.
- Full `medium` generation was prepared for local execution but not fully produced inside this chat environment because the dataset is intentionally much larger.
